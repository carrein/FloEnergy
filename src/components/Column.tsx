import styled from "styled-components";

// Generic layout component to be extended with additional styles.
export const Column = styled.div`
  display: flex;
  flex-direction: column;
  gap: 8px;
`;
